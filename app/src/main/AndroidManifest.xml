<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maryow.assessment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.movie.MovieReviewActivity"></activity>
        <activity android:name=".activity.movie.MovieDetailActivity" />
        <activity android:name=".activity.movie.MovieActivity" />
        <activity android:name=".activity.news.SearchActivity" />
        <activity android:name=".activity.news.NewsDetailActivity" />
        <activity android:name=".activity.news.NewsActivity" />
        <activity android:name=".activity.news.SourceActivity" />
        <activity
            android:name=".activity.news.SplashScreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.news.MainActivity" />
    </application>

</manifest>